# Certificate images

Put certificate images in this folder. Use clear, web-friendly filenames, such as:

`aws-cloud-practitioner.png`

Then add a matching entry to `src/components/CertificationsSection.tsx`, using the image path:

`/certificates/aws-cloud-practitioner.png`

Use PNG, JPG, or WebP. WebP is preferred for large images.
